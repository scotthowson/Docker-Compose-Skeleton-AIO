{
  "name": "nextcloud",
  "title": "Nextcloud",
  "description": "Nextcloud self-hosted file storage, sync, and collaboration platform",
  "category": "storage",
  "target_stack": "storage-backup",
  "icon": "☁️",
  "tags": ["storage", "file-sync", "collaboration", "cloud", "nextcloud"],
  "variables": [
    {
      "name": "HOST_PORT",
      "label": "Web UI Port",
      "description": "Port exposed on the host for the Nextcloud web interface",
      "default": "8080",
      "required": false
    }
  ]
}
