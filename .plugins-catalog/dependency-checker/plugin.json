{
  "name": "dependency-checker",
  "version": "2.0.0",
  "author": "DCS Community",
  "category": "advanced",
  "description": "Before a deployment: resolves the template's depends_on graph with docker compose and reports circular references, missing targets, and dependencies on services without a health check (which makes condition: service_healthy impossible).",
  "tags": [
    "dependencies",
    "compose",
    "validation"
  ],
  "env": [],
  "config": {},
  "hooks": [
    "pre-deploy"
  ],
  "contract": 2
}
